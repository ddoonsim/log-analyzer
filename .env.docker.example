# =============================================================
# Docker Compose 환경 변수
# =============================================================
# 사용법:
#   1. 이 파일을 복사: cp .env.docker.example .env
#   2. 아래 값을 실제 운영 값으로 변경
#   3. ./init-ssl.sh          (최초 1회 - SSL 인증서 발급)
#   4. docker compose up -d   (이후 시작/재시작)
# =============================================================

# [필수] 도메인 (SSL 인증서 발급 대상)
DOMAIN=your-domain.com

# [필수] Let's Encrypt 알림 이메일
CERT_EMAIL=your@email.com

# [필수] Anthropic Claude API Key
ANTHROPIC_API_KEY=sk-ant-xxxxx

# [필수] PostgreSQL 비밀번호 (강력한 비밀번호 사용!)
POSTGRES_PASSWORD=change-me-to-a-strong-password

# [선택] PostgreSQL 사용자명 (기본값: loganalyzer)
# POSTGRES_USER=loganalyzer

# [선택] HTTP/HTTPS 외부 포트
# HTTP_PORT=80
# HTTPS_PORT=443
